{
    "private": true,
    "type": "module",
    "scripts": {
        "refresh": "php artisan clear-compiled && composer dump-autoload && php artisan optimize",
        "clean": "php artisan config:cache && php artisan route:cache && php artisan view:cache",
        "build": "vite build",
        "dev": "vite"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^6.0"
    }
}
